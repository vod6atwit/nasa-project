import React from 'react';
// allow react code make use of document object models
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
  // app component using react syntax called Jsx
  <App />,
  document.getElementById('root')
);
